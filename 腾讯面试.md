# 腾讯面试题

## 1. 自我介绍

我会先用一分钟讲清楚三个点：我做了几年、主要技术栈是什么、最近最匹配这个岗位的项目是什么。

比如我会说：“我主要做前端方向，核心技术栈是 Vue / React / TypeScript。最近的项目里，我主要负责某个核心模块，从需求拆解、组件设计到性能优化都有参与。比较有代表性的成果是解决了某个体验或性能问题，比如首屏、表单复杂度、富文本、权限或者工程化效率。整体上，我的优势是能把业务需求拆成稳定可维护的前端实现，也比较关注工程质量和协作效率。”

重点不是把经历按时间线念一遍，而是尽快让面试官知道：我是谁、我做过什么、为什么适合这个岗位。

## 2. Vue 2 和 Vue 3 的区别

先说核心区别：Vue 2 的响应式主要靠 `Object.defineProperty`，Vue 3 换成了 `Proxy`，所以 Vue 3 对新增属性、删除属性、数组变化这些场景支持得更自然。

然后补两点：第一，Vue 3 引入了 Composition API，复杂逻辑更容易按功能组织，也更适合 TypeScript。第二，Vue 3 在编译和运行时也做了优化，比如静态提升、Patch Flag、Tree Shaking，所以性能和包体积都有更好的空间。

如果一句话总结，就是：Vue 3 不只是 API 变了，它在响应式、逻辑组织、类型支持和性能优化上都比 Vue 2 更现代。

## 3. Composition API 相比 Options API 的优势

Composition API 最大的好处是：同一块业务逻辑可以写在一起，也更容易抽成可复用的组合式函数。

Options API 是按 `data`、`methods`、`computed` 这种选项来分的，组件简单时很清楚，但组件一复杂，同一个功能的状态、方法和副作用就会分散在不同位置。Composition API 可以按功能来组织，比如搜索逻辑、分页逻辑、权限逻辑各自放在一起，后期维护会舒服很多。

另外它不依赖 `this`，TypeScript 推断也更直接。不过我不会说 Options API 不好，简单组件用 Options API 也完全没问题，主要看复杂度和团队规范。

## 4. Vue 2 新增属性除了 `$set` 怎么处理？

除了 `this.$set`，也可以用 `Vue.set`，本质上是一样的，都是让新增属性也能被 Vue 2 的响应式系统追踪到。

还有一种常见做法是整体替换对象，比如：

```js
this.user = { ...this.user, age: 18 };
```

因为对象引用变了，视图也能更新。更推荐的做法是，如果字段结构一开始就知道，就提前在 `data` 里声明好，避免后面动态补字段。Vue 3 因为用了 `Proxy`，这个问题就少很多，直接新增属性通常就可以响应。

## 5. Pinia 相比 Vuex 有什么优势？

Pinia 的优势主要是更轻、更直观，写起来也更接近普通函数。

Vuex 里通常要区分 state、getter、mutation、action，流程比较重；Pinia 不需要 mutation，直接在 action 里改状态就行。它对 TypeScript 的推断也更好，store 拆分更自然，开发工具体验也不错。

我一般会这样判断：Vue 3 新项目优先 Pinia；但不是所有状态都要放全局，只有跨页面、跨组件、需要持久化或统一管理的状态才适合进 store。

## 6. 不用第三方库怎么做简单全局状态？

如果只是简单全局状态，我会用 Vue 自己的响应式能力来做。

比如在一个模块里定义 `reactive` 状态，再通过一个 `useAppState` 暴露只读状态和修改方法：

```js
const state = reactive({ user: null });

export function useAppState() {
  const setUser = user => {
    state.user = user;
  };

  return { state: readonly(state), setUser };
}
```

这样做的重点是：状态可以全局共享，但修改入口要收敛，不能让任何组件都随便改。局部层级共享也可以用 `provide/inject`。如果后面出现持久化、插件、SSR 隔离、复杂调试这些需求，再上 Pinia 会更合适。

## 7. 解构和展开为什么破坏响应式？

因为 `reactive` 返回的是一个 Proxy，响应式依赖是在访问这个 Proxy 属性时收集的。你直接解构出来以后，拿到的就是普通值，不再经过原来的 Proxy，所以响应式链路就断了。

比如：

```js
const { name } = reactiveUser;
```

这个 `name` 就不是响应式引用了。解决方式一般是用 `toRef` 或 `toRefs`，或者干脆一直通过原对象访问：

```js
const { name } = toRefs(reactiveUser);
```

展开也是类似，它得到的是一个普通浅拷贝。如果再包一层 `reactive`，那也是一份新的响应式对象，和原对象不是同一套状态。

## 8. watch 和 watchEffect 的原理及异步问题

这样区分：`watch` 是我明确告诉 Vue 监听谁，变化后再执行回调；`watchEffect` 是先执行一遍函数，执行过程中用到哪些响应式数据，Vue 就自动收集哪些依赖。

所以 `watch` 更适合明确的数据源，比如监听搜索关键词、路由参数、分页条件；`watchEffect` 更适合依赖比较简单、想少写一点监听源的场景。

异步里要注意一个点：`watchEffect` 只会收集第一次同步执行阶段访问到的依赖，`await` 之后再访问的响应式数据不一定会被追踪。做请求时我更倾向用 `watch`，然后在清理回调里取消旧请求，避免前一个慢请求覆盖后一个新结果。

## 9. Observer 对象是什么？

这个问题要先看语境。

如果说的是浏览器里的 Observer，一般是 IntersectionObserver、ResizeObserver、MutationObserver 这类 API。它们的作用是观察目标变化，比如元素进入视口、尺寸变化、DOM 变化，然后异步通知我们，比手动轮询更高效。

如果说的是 Vue 2 源码里的 Observer，它负责把对象转成响应式对象：遍历属性，用 getter/setter 做依赖收集和派发更新。

所以我会先跟面试官确认：“您这里指的是浏览器 Observer API，还是 Vue 响应式里的 Observer？”

## 10. 隐私接口加密和密钥管理

我会先说底线：前端不能保存真正的长期密钥，浏览器里的代码、环境变量和混淆都不可靠，核心密钥必须放服务端或 KMS。

正常传输首先要靠 HTTPS。如果还需要字段级加密，可以做混合加密：前端生成临时 AES 密钥加密敏感字段，再用服务端下发的短期公钥加密这个 AES 密钥，服务端用私钥解开。这样既兼顾性能，也不会把长期私钥暴露给前端。

除此之外还要考虑防重放、时间戳或 nonce、密钥过期和轮换、日志脱敏，以及服务端权限校验。加密只是其中一层，不能替代鉴权和风控。

## 11. Git submodule 怎么优化？

我会先判断这个 submodule 的真实用途。

如果它是一份公共能力，而且版本可以独立发布，那我更倾向迁成内部 npm 包，这样版本管理、发布、回滚都会清楚很多。如果多个项目经常一起改、一起发，那 monorepo 可能更合适，可以统一依赖和构建流程。

迁移时不能一次性全替换。我会先梳理依赖关系和版本边界，再做双轨验证：新包或新目录先接入一两个项目跑通，确认构建、测试和发布没问题，再逐步替换，并保留回滚方案。

## 12. 富文本怎么准确统计字符？

我会先明确规则，因为富文本统计没有绝对标准。比如图片算不算一个字符，表格怎么统计，换行和空格算不算，emoji 算一个还是多个，这些都要产品先定清楚。

实现上，我优先遍历编辑器自己的文档模型，比如 ProseMirror、Slate、Quill 这类都有结构化数据。不要直接拿 `innerHTML.length`，那会把标签也算进去。退一步才是解析安全 DOM，只统计文本节点。

还有一个细节是 Unicode，不能简单用字符串长度，因为 emoji 或一些组合字符会被算错。更准确的方式是按字素统计，比如用 `Intl.Segmenter` 或类似方案。图片、表格、mention 这类特殊节点就按产品规则单独处理。

## 13. Vite 相比 Webpack 的优势

Vite 最大的优势在开发体验，尤其是启动和热更新更快。

它开发环境基于浏览器原生 ESM，源码按需转换，不需要一开始就把整个项目打包完；依赖会预构建，所以冷启动和 HMR 通常都比传统 Webpack 项目更轻。生产环境默认用 Rollup 打包，也能做 Tree Shaking 和代码拆分。

但我不会简单说 Vite 一定替代 Webpack。Webpack 的生态和定制能力还是很强，老项目、复杂 loader、特殊构建链路可能继续用 Webpack 更稳。选型主要看项目规模、插件生态、兼容要求和迁移成本。

## 14. 微前端原理，为什么要选用微前端
## 15. 小程序怎么实现动画，帧动画怎么实现
## 16. 图片预加载怎么实现，
## 17. 小程序怎么实现虚拟列表
## 18. 小程序消息订阅，一次性和长期订阅
## 19. 小程序进来怎么实现默认登陆
## 20. AI使用的流程
## 21. AI的知识图谱怎么使用，他的原理是什么